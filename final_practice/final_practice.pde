//might make a piano
//may end up changing to a different instrument but will otherwise be making a "game" that produces sound
//maybe will play a tune then make the user repeat the tune??

void setup(){
  size(900,900,P3D);
  background(255);
  stroke(0);
  
}

void keyPressed()
{
  SineWave sine;
  Notes note;

  float pitch = 0;
if (pitch > 0) {
      note = new Notes(pitch, 0.5);
   }
}